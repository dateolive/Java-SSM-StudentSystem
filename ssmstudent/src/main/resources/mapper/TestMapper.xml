<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.datealive.mapper.Testmapper">

    <sql id="Base_column">
        id, sn, name, password, clazz_id, sex, mobile, qq
    </sql>

    <select id="queryStudentTest" resultType="com.datealive.entity.Test">
        select * from s_student where name = #{name} and password = #{password};
    </select>





</mapper>